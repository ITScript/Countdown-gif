language: php

php:
  - '5.6'
  - '7.0'
  - '7.1'

before_install:
    - add-apt-repository -y ppa:moti-p/cc
    - apt-get update
    - apt-get -y --reinstall install ghostscript
    - apt-get -y --reinstall install gs-esp
    - apt-get -y --reinstall install imagemagick
    - printf "\n" | pecl install imagick-beta

install:
  - composer require --dev
